# Safety and Governance

Project Chimera must enforce strong governance.

Rules:

- All outputs must pass through Judge agents
- Unsafe outputs must be rejected
- Uncertain outputs must escalate to HITL (Human-in-the-Loop)
- Budget limits must be enforced for transactions
- Sensitive data must never be stored in source code

Judge agents enforce all safety policies.